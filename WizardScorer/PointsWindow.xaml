<Window x:Class="WizardScorer.PointsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WizardScorer"
        mc:Ignorable="d"
        Title="PointsWindow" Height="600" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel x:Name="Toolbar" Grid.Row="0" Orientation="Horizontal">
                <Button x:Name="To_predictions" Width="200" Margin="100,0" HorizontalAlignment="Left" Click="To_predictions_Click">Vissza a jóslatokhoz</Button>
                <Button x:Name="To_tricks" Width="200" Margin="100,0" HorizontalAlignment="Right" Click="To_tricks_Click">Vissza az ütésekhez</Button>
            </StackPanel>
        </Grid>
        <Grid x:Name="PointsGrid" VerticalAlignment="Center" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button x:Name="New_round" Width="200" Click="New_round_Click">Gomb</Button>
        </Grid>
        
    </Grid>
</Window>
